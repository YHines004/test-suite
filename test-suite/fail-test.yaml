queue: dev
jobSetId: validation-suite
jobId: fail-test
priority: 10

podSpec:
  containers:
  - name: fail
    image: busybox
    command: ["sh","-c","echo Failing job; exit 1"]
  restartPolicy: Never